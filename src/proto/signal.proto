syntax = "proto3";

package proto;

enum Signal {
    SIGNAL_UNSPECIFIED = 0;
    SIGNAL_START = 1;
    SIGNAL_QUIT = 2;
    SIGNAL_NEXT_TETRO_REQUEST = 3;
}

message SignalMessage {
    Signal signal = 1;
}
